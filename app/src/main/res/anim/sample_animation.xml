<?xml version="1.0" encoding="utf-8"?>

 <scale xmlns:android="http://schemas.android.com/apk/res/android"
    android:duration="1000"
    android:fromXScale="1.0"
    android:fromYScale="1.0"
    android:toXScale="0.0"
    android:toYScale="0.0"
    android:pivotX="0.5"
    android:pivotY="0.5"/>

    <!--

    scale -> A resizing animation.
    You can specify the center point of the image from which it grows outward (or inward)
     by specifying pivotX and pivotY. For example, if these values are 0, 0
     (top-left corner), all growth will be down and to the right.
      Represents a ScaleAnimation.

    fromX -> Horizontal scaling factor to apply at the start of the animation
    toX -> Horizontal scaling factor to apply at the end of the animation
    fromY -> Vertical scaling factor to apply at the start of the animation
    toY -> Vertical scaling factor to apply at the end of the animation
    pivotX -> The X coordinate to remain fixed when the object is scaled.
    pivotY ->  The Y coordinate to remain fixed when the object is scaled.
    duration -> Animation duration.
    -->
